# Ridge & Rowan Dental — Cloudflare Pages Redirects
# Rewrite /assets/* paths to actual /client/assets/* location (internal 200 rewrite)
/assets/*  /client/assets/:splat  200

# The TanStack Start Worker (_worker.js) handles SSR for all other routes.
# Static assets under /client/assets/ are excluded from Worker via _routes.json.
